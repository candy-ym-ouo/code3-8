# 代码审查与修复报告

## 审查范围

- React 前端、Node.js API、BullMQ Worker。
- PostgreSQL/Prisma 数据一致性和迁移。
- Redis 提醒并发、重试和恢复。
- MinIO/S3 照片上传、缩略图、签名访问和生命周期。
- Docker Compose、环境变量和全新环境启动。

## 已修复问题

### 1. 查询参数布尔值解析错误

`z.coerce.boolean()` 会把字符串 `"false"` 转成 `true`，导致
`includeArchived=false`、`includeInactive=false`、`unreadOnly=false`
行为相反。已改为严格布尔查询解析，并增加单元测试。

### 2. 删除冷静期无法重新登录

用户申请删除后状态为 `DELETING`，旧登录逻辑只允许 `ACTIVE`，与“冷静期内登录并撤销删除”的流程冲突。现在允许 `DELETING` 用户登录，同时页面显示删除冷静期警告。

### 3. 植物与位置关联不一致

- 观察可关联到其他位置的植物，已改为拒绝并返回 `PLANT_ZONE_MISMATCH`。
- 非搬动操作关联位置时，植物当前位置必须一致。
- 搬动操作必须同时指定植物和目标位置。
- 直接修改植物位置时现在会写入 `PlantZoneHistory`。

### 4. 植物状态被旧记录覆盖

补录旧观察时不再直接覆盖植物当前状态。系统会按最新观察时间重新计算植物状态；删除或修改最新观察后也会重新计算。

### 5. 搬动记录被删除后位置不一致

搬动操作现在不可删除，执行时间也不可修改，避免删除历史后植物当前位置与历史记录冲突。需要更正时创建新的搬动记录。

### 6. 照片重复上传导致关联丢失

原逻辑遇到相同 SHA-256 会直接返回旧照片，新观察/操作无法关联该照片。现在同一图片可以形成独立照片记录和独立对象键，同时保留哈希索引用于查询。

### 7. 照片关联校验不完整

- 照片不能同时主关联观察和操作。
- 观察、操作、植物、位置必须属于同一空间。
- 显式位置与观察/操作位置冲突时会被拒绝。
- 直接关联植物和位置时，两者必须一致。
- 修改照片关联时会自动校正关联记录中的植物和位置。
- 软删除照片时会清除植物封面引用。

### 8. 照片排序错误

无拍摄时间的照片不再排在时间轴最前方。查询按 `capturedAt` 排序，并将空值放在最后，再按上传时间排序。

### 9. 提醒任务并发和恢复问题

- 提醒事件现在可以幂等恢复：任务在创建事件后崩溃时，下一次扫描会继续发送而不是永久漏发。
- 通知增加唯一去重键，Worker 并发扫描不会产生重复站内信。
- 稍后提醒使用 `deliveryVersion`，会生成新的未读通知，而不是复用已读通知。
- 邮件通知增加处理中状态、超时回收和待发送扫描，Worker 重启后可恢复。
- 阈值提醒按分钟生成稳定去重键，避免并发任务产生重复提醒。
- 完成提醒时，操作记录和提醒状态更新在同一事务中，重复提交不会重复生成操作记录。

### 10. 提醒配置校验

- 校验 IANA 时区。
- 植物状态阈值只能使用四个合法状态。
- 温度阈值必须为数字。
- 重新启用周期提醒时会重新计算下次执行时间。
- 前端支持温度数字阈值和植物状态枚举阈值两种输入。

### 11. 时间与状态校验

- 观察、操作和照片拍摄时间不能晚于当前时间 5 分钟。
- 删除操作中的空备注可以正确清空。
- 修改操作时间时会重新校验完成时间不能早于开始时间。
- 植物获取时间不能位于未来。

### 12. 删除与归档一致性

- 删除观察或普通操作时，其照片同步进入软删除。
- 归档植物时自动停用该植物提醒。
- 归档位置前若仍有活动植物，会拒绝并提示先搬动或归档植物。
- 归档阳台会同时归档其位置和植物，并停用相关提醒。

### 13. 分析接口

- Dashboard 不再向当前用户展示其他成员的提醒。
- 前后窗口覆盖度分别按各自窗口计算。
- 曲线最多返回 500 个点，超出时进行确定性下采样。
- 校验时间范围和对象所属空间。
- 干预对比可识别无拍摄时间但已上传的照片。

### 14. 前端表单和会话问题

- 首次配置的楼层改为数字输入，光照改为枚举选择。
- 观察和操作表单在更换阳台或位置时清理不再有效的下级选择。
- 搬动操作可以选择同阳台内其他位置的植物。
- 登录失效时自动返回登录页，但 `/auth/me` 的未登录状态不会把注册页错误重定向到登录页。
- 删除冷静期在主导航上方显示明确警告。

### 15. Docker 环境变量

Compose 现在会把 SMTP、上传大小、导出保留时间、默认时区和对象存储配置传给 API 与 Worker，`.env` 中配置邮件或外部对象存储时能够实际生效。

## 验证结果

已执行：

```bash
npm run typecheck
npm run build
npm test
npm audit --omit=dev --audit-level=high
docker compose config --quiet
docker compose build migrate api worker web
```

结果：

- TypeScript 类型检查通过。
- 前端生产构建通过。
- 10 个单元测试通过。
- 生产依赖审计为 0 个漏洞。
- Docker 镜像全部构建成功。
- 从全新 PostgreSQL、Redis、MinIO volume 启动成功。
- 3 个 Prisma 迁移全部成功。
- `/api/v1/health/ready` 返回 `ready`。
- Nginx、API、Worker、PostgreSQL、Redis、MinIO 全部健康。
- 最终数据库为 0 个用户，无测试或演示数据。

---

## 第二轮深度审查补充

### 16. 宿主机与容器环境变量冲突

旧配置在复制 `.env.example` 后会让容器读取指向 `localhost` 的数据库、Redis 地址，导致 Compose 启动失败。现已拆分：

- 宿主机开发：`DATABASE_URL`、`REDIS_URL`
- 容器内部：`DATABASE_INTERNAL_URL`、`REDIS_INTERNAL_URL`
- 本地开发 URL：`APP_BASE_URL`、`API_BASE_URL`
- Compose 公开 URL：`COMPOSE_APP_BASE_URL`、`COMPOSE_API_BASE_URL`
- S3 内外网地址：`S3_ENDPOINT`、`S3_INTERNAL_ENDPOINT`、`S3_PUBLIC_ENDPOINT`

### 17. 历史记录无法补录

旧逻辑用植物当前位置验证所有历史观察和操作，植物搬动后无法补录搬动前数据。现在通过 `PlantZoneHistory` 计算观察或操作发生时的位置；修改历史时间时也会重新校验。

### 18. 历史数据错误触发阈值提醒

补录旧观察不再触发当前阈值提醒；只有成为目标范围最新观察的触发记录才会评估。编辑最新观察的温度或植物状态时，会重新进入阈值评估队列。

### 19. 停用提醒后仍可能继续发送

停用提醒或删除提醒时，会同时终止 PENDING、SNOOZED 事件；Worker 发送前会再次确认提醒仍处于启用状态。

### 20. 照片隐私与文件安全

照片不再原样保存。JPEG、PNG、HEIC、AVIF 等输入都会转码为无 EXIF 的 WebP 原图，并限制总解码像素，防止多帧图片绕过像素上限。缩略图使用转码后的安全原图生成。

### 21. 账号删除安全

发起账号删除现在必须再次验证当前密码，错误密码不会注销会话或触发登录跳转。重复发起删除不会不断延长冷静期。

### 22. 关联照片删除一致性

删除观察或操作时，其照片会软删除，同时清除植物对已删除照片的封面引用。

### 23. 位置名称重复

同一阳台下未归档位置不能使用同名名称，创建和改名都有服务端校验。

### 24. 提醒配置边界

分钟级提醒不能设置固定时刻，星期配置只能用于周级提醒，重新启用单次提醒必须提供未来时间。

### 25. 前端提交一致性

观察或操作已保存但照片上传失败时，不再把整次提交显示为失败，避免用户重复创建记录；页面会明确提示记录已成功、照片上传失败。

### 26. 容器健康判断

API 容器健康检查从仅检查进程存活改为完整 readiness 检查，确保数据库、Redis 和对象存储都可用后 Web 才启动。

## 第二轮验证

- TypeScript 类型检查通过。
- 前端生产构建通过。
- 共享层、API、前端单元测试全部通过。
- 生产依赖审计为 0 个漏洞。
- 历史位置补录和跨搬动时间校验已通过真实数据库测试。
- 阈值提醒的“旧数据不触发、最新数据编辑后触发”已通过真实 Worker 测试。
- WebP 转码、无 EXIF 原图和签名 URL content-type 已验证。
- 密码错误拒绝删除、正确密码进入冷静期、冷静期内重新登录已通过。
- Docker Compose 使用 `.env.example` 可正确解析内部服务地址。
